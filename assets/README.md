# assets/

Source design assets that are **not** bundled into the build:
Figma exports, raw photography, logo source files, brand guidelines.

Anything that must be served to the browser belongs in `public/` instead.
